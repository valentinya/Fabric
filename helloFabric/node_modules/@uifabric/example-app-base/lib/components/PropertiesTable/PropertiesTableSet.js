"use strict";
var __extends = (this && this.__extends) || function (d, b) {
    for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
    function __() { this.constructor = d; }
    d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
};
var React = require("react");
var index_1 = require("../../utilities/parser/index");
var PropertiesTable_1 = require("./PropertiesTable");
var index_2 = require("../../utilities/parser/index");
var PropertiesTableSet = (function (_super) {
    __extends(PropertiesTableSet, _super);
    function PropertiesTableSet(props) {
        var _this = _super.call(this, props) || this;
        var componentName = props.componentName, componentPath = props.componentPath, sources = props.sources;
        var src;
        var properties = [];
        if (sources) {
            src = '';
            sources.forEach(function (source) { return src += source; });
        }
        else if (componentPath && componentName) {
            src = require(componentPath + componentName + '.Props.ts');
        }
        else {
            throw new Error('PropertiesTableSet was used without source or a componentPath/name');
        }
        if (props.renderOnly) {
            props.renderOnly.forEach(function (item) {
                properties = properties.concat(index_2.parse(src, item));
            });
        }
        else {
            properties = index_2.parse(src);
        }
        _this.state = {
            properties: properties
        };
        return _this;
    }
    PropertiesTableSet.prototype.renderEach = function () {
        var _this = this;
        return this.state.properties.map(function (item) {
            return (React.createElement(PropertiesTable_1.PropertiesTable, { key: item.propertyName, title: item.name === ('I' + _this.props.componentName) ? (_this.props.componentName + ' class') : item.propertyName, properties: item.property, renderAsEnum: item.propertyType === index_1.PropertyType.enum }));
        });
    };
    PropertiesTableSet.prototype.render = function () {
        return (React.createElement("div", null, this.renderEach()));
    };
    return PropertiesTableSet;
}(React.Component));
PropertiesTableSet.defaultProps = {
    title: 'Properties'
};
exports.PropertiesTableSet = PropertiesTableSet;

//# sourceMappingURL=PropertiesTableSet.js.map
